// Copy this file to config.js and fill in the values.
//
// IMPORTANT: config.js is served to every visitor of the dashboard, so the
// token here is PUBLIC. Use a dedicated Airtable personal access token with
// ONLY the data.records:read scope, restricted to ONLY this base.
// Never put the collector's read-write token here.
window.DASHBOARD_CONFIG = {
  AIRTABLE_TOKEN: "patXXXXXXXXXXXXXX.xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
  AIRTABLE_BASE_ID: "appXXXXXXXXXXXXXX",
};
