# Secrets — never commit
.env
.env.*

# Python
__pycache__/
*.pyc
.venv/
venv/

# Node (if the dashboard build uses it)
node_modules/

# OS junk
.DS_Store
Thumbs.db

# Local scratch
*.log

# Dashboard config holds an Airtable token — replace with a read-only token
# and remove this line before publishing to GitHub Pages
docs/config.js

# Internal analytics exports — kept locally, not published in the repo
csv/

# Local editor/agent config
.claude/
